import 'package:flutter/material.dart';

// TODO: Implement validation detail screen
// - Full photo display
// - Claimed price, item, store info
// - Confirm / Flag buttons
// - Optional reason field for flags

class ValidationDetailScreen extends StatelessWidget {
  const ValidationDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Review Submission')),
      body: const Center(child: Text('Validation Detail — TODO')),
    );
  }
}
