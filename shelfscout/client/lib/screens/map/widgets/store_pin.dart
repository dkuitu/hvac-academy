import 'package:flutter/material.dart';

// TODO: Implement custom map pin widget
// States: unscouted (grey), scouted (colored), crowned (gold), contested (red pulse)

class StorePin extends StatelessWidget {
  const StorePin({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(); // TODO
  }
}
