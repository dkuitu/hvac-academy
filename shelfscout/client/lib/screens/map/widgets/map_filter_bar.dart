import 'package:flutter/material.dart';

// TODO: Implement map filter bar
// Allows filtering store pins by item type (e.g., show only stores with Milk submissions)

class MapFilterBar extends StatelessWidget {
  const MapFilterBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(); // TODO
  }
}
